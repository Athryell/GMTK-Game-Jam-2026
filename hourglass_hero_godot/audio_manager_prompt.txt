I have an Audio autoload (audio.gd) that currently loads audio streams from 
res://audio/sfx and res://audio/music by resolving them from the file name, with 
volume_db and pitch_jitter passed as runtime arguments.

I want to change approach: instead of loading streams via code, I want a "SoundBank" 
scene with one AudioStreamPlayer per sound, so I can tweak volume_db and pitch_scale 
DIRECTLY in the Inspector, by hand, without any custom tooling.

PART 1 — SoundBank scene
Create res://audio/sound_bank.tscn with a root Node called SoundBank, and two container 
groups as children:
  - A Node "Sfx" container
  - A Node "Music" container
Under each, I will manually add AudioStreamPlayer nodes (one per sound, node name = id, 
e.g. "sword_hit", "boss_theme"), each with its bus already set (SFX or Music) and its 
stream assigned in the Inspector. Do NOT generate players for existing sounds via code — 
leave that as manual work for me; the scene only serves as a container + lookup source.

PART 2 — Rewrite audio.gd to use SoundBank
- Add @onready var _bank: Node = preload("res://audio/sound_bank.tscn").instantiate() 
  (or similar — decide whether it should be a separate autoload or a child instantiated 
  in _ready)
- sfx(sound_name: String) → look up Sfx/<sound_name>; if it doesn't exist, warn and 
  return, otherwise play THAT player with its OWN volume_db/pitch_scale/bus already set 
  in the Inspector (no override parameters for now — the values live only in the scene)
- To allow overlapping of rapidly repeated sounds (e.g. gunshots), when play() is called 
  on a player that's already playing, consider either duplicating the player on the fly 
  (player.duplicate()) so the previous sound isn't cut off, or handling it with a 
  per-sound pool if that makes more sense — tell me which approach is cleaner here.
- play_music(track_name: String, fade := 0.8) → same lookup logic under Music/<name>, 
  keep the fade/crossfade logic I already have (only one music player active at a time, 
  so here the bank player itself is NOT played directly — instead its .stream is read and 
  passed to the music player managed by the autoload). Decide whether it makes more sense 
  to play the bank player directly or just use the bank as a "stream + parameters library" 
  for music, given that fading and track switching are needed.
- Remove the folder/name/extension-based loading (_stream(), EXTENSIONS, etc.) since 
  streams are now assigned in the Inspector inside SoundBank instead of being loaded 
  at runtime by name

PART 3
Keep all the bus/volume/settings management in the file exactly as it is — it's unrelated 
to this change.

The project is Godot 4.x, GDScript, static typing. Keep the existing comment style 
(explain the "why", not the "what").
